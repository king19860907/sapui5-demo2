<mvc:View
        controllerName="sap.m.sample.header.controller.Header"
        xmlns:mvc="sap.ui.core.mvc"
        xmlns:tnt="sap.tnt"
        xmlns:core="sap.ui.core"
        xmlns="sap.m">
    <tnt:ToolHeader>
        <ToolbarSpacer />
        <Button id="loginInfo" text="" type="Transparent" press="handleUserNamePress">
            <layoutData>
                <OverflowToolbarLayoutData priority="NeverOverflow" />
            </layoutData>
        </Button>
    </tnt:ToolHeader>
</mvc:View>